# 5.1 会话与情绪化构式的语法文体：独立教学页质量报告

状态：`REVIEW`。原书 OCR 风险仍保留为 `needs-review`，学习页可见状态为“待复核”。

## 来源与结构

- 原书规则和例句：印刷页 115-116，PDF 117-118；每段均链接到规则单元中的逐字俄文和页码。
- 四个教学阶段按语用动作组织：重复/被迫、情绪否定/聚焦、接受/让步、评价/资格不足/条件缺失。
- 每阶段包含语境预测、三段中文拆解、原书规则、原书例句、最小对比、信号边界、三类误判、两道随堂题及可作答同类再练。

## 正式题与迁移

- 22 道 mapped 正式题 `GL5-Q118` 至 `GL5-Q139` 均仅使用既有 ID，并按四阶段折叠呈现。
- 正式题继续写入 `rr_b2_progress_v1`；随堂判断、重试、最终判断和四项迁移任务只写入 `rr_zlatoust_learning_v1`。
- 迁移任务覆盖新语境判断、改写条件、中文解释与禁止/条件缺失的边界判断。

## 风险与验收

- 不把“不能做”混成禁止，不把 `тоже мне` 与 `а туда же` 混成同一评价结构。
- 页码、正式题映射和 OCR 风险由项目验证器检查；浏览器验收覆盖卡片入口、错误反馈、重试、正式题存储隔离和 390px 视口。
