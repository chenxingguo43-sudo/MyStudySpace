# 2.3 独立教学页验收记录

日期：2026-07-26
状态：`REVIEW`（专项结构与静态检查通过；浏览器验收见本记录末尾；OCR 风险保留）

- 内容：5 个连续阶段依次处理伴随感受、伴随动作与抽象态度、动作方式的质性特征、“删定语”诊断，以及固定搭配与题目来源边界。导图是交叉判断轴，不把 `с + Тв. п.` 和无介词 `Тв. п.` 画成机械二选一。
- 来源：原书 2.3，印刷页 100 / PDF 102。每个阶段的逐字规则、原书例项和来源元数据均携带印刷页与 PDF 页；教辅判断语、自编即时题和正式题语境明确分层。
- 正式题：嵌入 12 道已映射题 `GL2-Q037`、`Q038`、`Q040–Q044`、`Q047–Q049`、`Q141`、`Q143`，均只使用 `rr_b2_progress_v1`。
- 边界：`GL2-Q036` 保持 `needs-review`；`GL2-Q039`、`Q045`、`Q046`、`Q139` 保持 `source-exercise-only`。它们不渲染为本页正式题，也不从正确选项反推 2.3 原书规则。
- 闭环：每阶段均含语境预测、老师拆解、逐字证据、最小对比、即时判断、包含误判类型/失效原因/回看位置/最小对比的反馈，以及可作答的同类补救题。正式题放在阶段内可折叠练习区。
- 迁移：覆盖新语境判断、改变关系后改写、中文解释“删定语”诊断，以及待复核来源边界。
- 兼容性：正式题继续复用既有 `GL2-Qxxx` ID 与 `rr_b2_progress_v1`；随堂、补救与迁移只写入 `rr_zlatoust_learning_v1`，原有记录保持可读。
- 来源风险：原书正文来自 OCR 清洗源，页面可见状态为“待复核”，未标示为无风险通过。

## 验收命令

```powershell
node scripts/validate-zlatoust-grammar-project.js
node --test tests/russian-b2/reader-static.test.js
npx playwright test tests/reader-zlatoust-navigation.spec.js --grep "Zlatoust 2\\.3" --reporter=line
```

浏览器专项验收覆盖：第 2 章知识点卡片入口、5 阶段与 12 道正式题、错误后可作答补救、`GL2-Q041` 写入正式题存储、`GL2-Q036/Q039` 不被渲染为正式题，以及 390×844 下无横向溢出、控制项在视口内、5 个复习复选框无障碍名称唯一。
