# 1.4.2 独立教学页验收记录

日期：2026-07-26
状态：`REVIEW`（实施与本节浏览器验收完成；原书 OCR 风险保留）

## 教学结构

- 知识点卡片仍保留在第 1 章；点击 1.4.2 进入独立连续教学页，不在章节目录展开缩水内容。
- 学习路线只追踪三个阶段、完成度和薄弱项；思维导图单独采用顺序分支：先问是否正在检查具体结果，否定时再区分断然拒绝和一般未做。
- 三个阶段分别覆盖：否定动作事实、否定预期结果、断然否定动作事实。每段均有语境预测、老师拆解、原书逐字证据、逐句解析、最小对比、即时判断、错因反馈、同类再练与折叠正式题。
- 6/6 随堂判断都有独立可作答的 `retry`；四项迁移任务覆盖过去时判断、将来时区分、改写条件和中文解释。

## 来源与练习兼容

- 原书规则、六条原书例句均追溯到印刷页 95 / PDF 097；逐字证据只引用原书表格中的完整条目，摘要明确标为 `learning-note`。
- 原书正式题仅复用 `GL1-Q045`、`GL1-Q046`、`GL1-Q095`，仍走既有 `rr_b2_progress_v1`；随堂题与迁移任务保存在 `rr_zlatoust_learning_v1`。
- 来源状态在可见界面显示为“待复核”；内部 `needs-review` 状态未删除。
- 开放式迁移作答只记录完成状态，不伪造语言质量的自动判分。

## 本轮验收

- `node scripts/validate-zlatoust-grammar-project.js`：`REVIEW`，0 failures。
- `node --test tests/russian-b2/reader-static.test.js`：54/54 通过。
- 浏览器：从第 1 章知识点卡片进入 1.4.2；验证分支导图、三个阶段、折叠正式题、迁移任务和“标记复习”的独立无障碍名称。
- 浏览器：错误选择 `позвонил` 后，页面显示具体误判原因、回看规则、最小对比与同类再练。
- 移动端：390×844 下页面无横向溢出，导图三条路径均保持可点击宽度。

## 风险保留

- 原书页面经过本节核对，但全书 OCR 审核总状态仍为 `REVIEW`；不得将本节表述为来源无风险通过。
- 本节不触碰听说教材、音频媒体、`listening-workbench`、`cloudsync-config.js` 或 `package.json`。
