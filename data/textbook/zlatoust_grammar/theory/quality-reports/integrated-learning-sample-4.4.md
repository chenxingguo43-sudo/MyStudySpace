# 4.4 连词раз：独立教学页质量报告

状态：`REVIEW`。原书规则、例句及页码来自印刷页 115、PDF 117，OCR 风险仍保留为 `needs-review`。

## 教学闭环

- 入口先分逻辑论据与“条件加原因”两条意义路径。
- `раз ≈ если` 仅指逻辑依据且偏书面；`раз ≈ так как` 仅指条件义叠加原因义且偏口语。
- 两条路径均必须通过真实与一次性两个边界。五阶段均包含原书证据、对比、判断、错因反馈和同类再练。

## 正式题边界

- 4.4 没有 `mapped` 原书正式题，页面正式题数量为零，不伪造来源或进度。
- `GL4-Q062--Q065`、`GL4-Q071`、`GL4-Q076`、`GL4-Q077` 及 `GL4-Q087--Q102` 继续是 `source-exercise-only`。
- 随堂题、再练和四项迁移任务仅使用 `rr_zlatoust_learning_v1`，没有创建第二套正式题记录。
