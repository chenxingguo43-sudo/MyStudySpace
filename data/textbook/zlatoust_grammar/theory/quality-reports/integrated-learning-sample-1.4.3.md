# 1.4.3 独立教学页验收记录

日期：2026-07-26
状态：`REVIEW`（数据与静态验收完成；浏览器抽查待批次复验）

## 教学结构

- 卡片入口 `gl1-4-3` 已映射到独立教学页 1.4.3。
- 学习路线有四个阶段：过程、结果、多次、单次；思维导图只展示“动作轮廓”和“事件数量”两条可组合判断轴，明确不是四选一。
- 每阶段均含语境预测、三段老师拆解、原书逐字证据、页码例句、最小对比、信号边界、常见误判、两道即时判断、定向反馈、独立再练和折叠正式题。
- 8/8 即时题有 `retry`；四项迁移任务覆盖新语境判断、改写、中文解释时间词边界和“不虚构词汇触发词”的判断。

## 来源与兼容

- 原书规则与例句均追溯到印刷页 95 / PDF 097；摘要标为 `learning-note`，不冒充逐字内容。
- 正式题只复用 `GL1-Q047`、`GL1-Q052`–`GL1-Q055`、`GL1-Q061`，继续使用 `rr_b2_progress_v1`；教学题和迁移任务使用 `rr_zlatoust_learning_v1`。
- 页面范围明确排除 1.4.4 的词汇限制表：`уметь`、`учить`、`удалось`在本节按上下文的界限和次数判断，不被伪造为触发词。
- OCR 内部状态保留 `needs-review`，页面显示为“待复核”。

## 已完成验证

- 内联 `reader.html` 脚本解析通过。
- `node scripts/validate-zlatoust-grammar-project.js`：`REVIEW`，0 failures。
- `node --test tests/russian-b2/reader-static.test.js`：55/55 通过。
- `git diff --check`：本节相关改动无空白错误。

## 待批次复验

- 本轮两次尝试创建本地浏览器页面均未能附着到页面，未以自动测试代替页面交互验收。
- 恢复浏览器连接后，需抽查卡片入口、两轴导图的点击定位、正式题折叠、错误反馈与 390×844 无横向溢出。
