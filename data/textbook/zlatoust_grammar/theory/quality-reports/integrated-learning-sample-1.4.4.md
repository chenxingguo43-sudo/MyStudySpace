# 1.4.4 独立教学页验收记录

日期：2026-07-26
状态：`REVIEW`（完成数据、静态与实际页面抽查；OCR 风险保留）

## 教学结构

- 新增独立入口 `gl1-4-4-lexical`，不复用或覆盖原有的 1.4.5–1.4.8 合并卡片。
- 学习路线分为四个有限词群：阶段动词、习惯获得或丧失、负面态度/禁止/不合宜、`успеть`/`забыть`/`спешить` 的完成体组。
- 思维导图展示的是原书词汇限制的四个并列检索组；专用说明明确要求先核对是否在清单内，表外词回到 1.4.3 的事件语义判断，不把它误表述为时态闸门或通用于全部不定式的固定流程。
- 每个阶段都有语境预测、三段老师拆解、页码来源证据、两个最小对比、信号边界、常见误判、两道即时题、定向错因反馈、独立再练与折叠正式题。
- 8/8 即时题均有 `retry`；四项迁移任务覆盖清单核对、新语境判断、改写条件与中文解释。

## 来源与兼容

- 原书规则和例句均追溯到印刷页 95–96 / PDF 097–098；摘要保持 `learning-note`，逐字内容才使用 `source-rule` 或 `source-example`。
- 正式题只复用 `GL1-Q048`–`Q051`、`Q056`–`Q060`、`Q062`–`Q065`，继续使用 `rr_b2_progress_v1`；随堂题和迁移任务仅使用 `rr_zlatoust_learning_v1`。
- 页面明确排除 `уметь`、`учить`、`удалось` 等表外动词，不能将相似语义伪造为 1.4.4 触发条件。
- OCR 内部状态保留 `needs-review`；学习者界面通过既有状态转换显示“待复核”。

## 已完成验证

- JSON 抽查：4 个阶段、8 道即时题、8 个同类再练，以及 13 个不重复正式题 ID 均存在。
- `node scripts/validate-zlatoust-grammar-project.js`：`REVIEW`，0 failures。
- `node --test tests/russian-b2/reader-static.test.js`：56/56 通过。
- `git diff --check`：本节相关改动无空白错误。
- 实际页面抽查：章节卡片可打开 1.4.4；导图节点定位到阶段 1；阶段 1 的原书正式题可折叠展开并显示 `GL1-Q048`；390×844 下文档宽度 385px、无横向溢出。
