# 3.1.2 禁止使用动名副词：独立教学页质量报告

状态：`REVIEW`。本页忠实保留原书的不同主体、真无主谓词和被动结构限制，OCR 风险持续标记为 `needs-review`。

- 教学路径：两个行动者 → 真无主谓词 → 被动责任冲突 → 还原从句 → Q039 证据边界。
- 原书规则：印刷页 111 / PDF 113；每个阶段显示页码和原书证据。
- 正式题：0 道。映射中仅有 `GL3-Q039`，且状态为 `needs-review`，不能伪装成已安全映射的正式题。
- `GL3-Q039` 题面（印刷页 46 / PDF 48）与被动限制表面冲突，页面展示题面、来源和“待复核”，不写入 `rr_b2_progress_v1`。
- 随堂判断、同类再练与四类迁移任务仅写入 `rr_zlatoust_learning_v1`。

验收完成：项目验证器为 `REVIEW`、0 failures；静态测试通过；第 3 章浏览器回归验证了零正式题状态、Q039 待复核证据、同类再练、390 x 844 视口和五个独立复习控件名称。
