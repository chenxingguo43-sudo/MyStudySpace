# P2 丰富语法知识卡片验收记录

## 自动化验收

- `npm run test:russian-b2`：50/50 通过。
- `node --test tests/russian-b2/study-cards.test.js`：4/4 通过。
- `node --test tests/russian-b2/reader-static.test.js`：14/14 通过。
- `npm run build:russian-b2-study-cards`：构建通过。
- `git diff --check`：无内容错误（仅保留 Windows 换行提示）。

## 浏览器验收

在隔离的本地 `http://localhost:3001/reader.html` 中完成：

1. 从 P2 章节的“知识点导航”打开“持续、原因与时间关系”。
2. 确认“3 分钟速查”、8 个学习分节、知识点目录和“综合自测”均可见。
3. 确认原书考点、`新编俄语语法`、补充例句等来源标签与中俄例句可见。
4. 确认 `дождь → из-за дождя`、`ошибка → из-за ошибки` 等就地格变化可读。
5. 点击第一节即时检查的正确选项，结果在原检查区域显示，页面没有跳回顶部。
6. 确认答案默认隐藏，卡片仍保留“做本知识点全部题目”和原书题目入口。

## 结论

P2 丰富知识卡片达到当前设计目标，可作为后续 P1/P3–P6 知识点卡片的实现模板。
