# 沉浸式俄语学习舱 V9.0 — 数据存储说明

> 本文档记录所有 `localStorage` 键名、数据格式、读写位置及用途。
> 适用于：番茄钟模块 + 俄语口语素材库模块

---

## 一、番茄钟模块（pomodoro.html）

| 键名 | 数据类型 | 格式说明 | 写入位置 | 读取位置 | 用途 |
|------|-----------|-----------|----------|----------|------|
| `pomodoro-today-count` | Number | 整数，今日已完成番茄数 | pomodoro.html | study-stats.html | 统计今日番茄数 |
| `pomodoro-today-minutes` | Number | 整数，今日累计专注分钟 | pomodoro.html | study-stats.html | 统计今日专注时长 |
| `pomodoro-total-count` | Number | 整数，累计完成番茄总数 | pomodoro.html | study-stats.html | 统计累计番茄数 |
| `pomodoro-total-minutes` | Number | 整数，累计专注总分钟 | pomodoro.html | study-stats.html | 统计累计专注时长 |
| `pomodoro-streak` | Number | 整数，连续打卡天数 | pomodoro.html | study-stats.html | 统计连续学习天数 |
| `pomodoro-week-count` | Number | 整数，本周番茄数 | pomodoro.html | （预留） | 统计本周数据 |
| `pomodoro-month-count` | Number | 整数，本月番茄数 | pomodoro.html | （预留） | 统计本月数据 |
| `pomodoro-sessions` | Array | `[{date, start, end, duration, task}, ...]` | pomodoro.html | （预留） | 详细会话记录 |
| `pomodoro-current-task` | String | 当前任务名称 | pomodoro.html | pomodoro.html | 记录当前正在做的任务 |

### 数据格式详解

**`pomodoro-sessions` 格式：**
```json
[
  {
    "date": "2025-01-15",
    "start": "14:30:00",
    "end": "14:55:00",
    "duration": 25,
    "task": "复习俄语口语"
  }
]
```

---

## 二、俄语口语素材库模块（俄语知识库.html）

| 键名 | 数据类型 | 格式说明 | 写入位置 | 读取位置 | 用途 |
|------|-----------|-----------|----------|----------|------|
| `oral-read-records` | Object | `{row-id: {count, dates, lastRead}}` | 俄语知识库.html | study-stats.html, 俄语知识库.html | 记录阅读次数 |
| `oral-speak-records` | Object | `{row-id: {count, dates, lastSpeak, rating}}` | 俄语知识库.html | study-stats.html, 俄语知识库.html | 记录口语练习次数及评分 |
| `oral-memo-records` | Object | `{row-id: {count, dates, lastMemo}}` | 俄语知识库.html | study-stats.html, 俄语知识库.html | 记录记忆次数 |
| `oral-chapter-masks` | Object | `{chapter: {ru: bool, zh: bool}}` | 俄语知识库.html | 俄语知识库.html | 章节遮罩状态 |
| `oral-speak-daily` | Object | `{date: [{rowId, rating, timestamp}]}` | 俄语知识库.html | study-stats.html | 按日期记录口语评分 |
| `oral-daily-position` | Object | `{date: scrollPosition}` | 俄语知识库.html | 俄语知识库.html | 记录每日滚动位置 |

### 数据格式详解

**`oral-read-records` 格式：**
```json
{
  "row-1-1": {
    "count": 3,
    "dates": ["2025-01-15", "2025-01-16", "2025-01-17"],
    "lastRead": "2025-01-17"
  },
  "row-1-2": {
    "count": 1,
    "dates": ["2025-01-16"],
    "lastRead": "2025-01-16"
  }
}
```

**`oral-speak-records` 格式：**
```json
{
  "row-1-1": {
    "count": 2,
    "dates": ["2025-01-15", "2025-01-17"],
    "lastSpeak": "2025-01-17",
    "rating": 4
  }
}
```
> `rating` 值：1=😰 困难, 2=😐 一般, 3=🙂 不错, 4=😊 很好, 5=🌟 完美

**`oral-memo-records` 格式：**
```json
{
  "row-1-1": {
    "count": 5,
    "dates": ["2025-01-15", "2025-01-16", "2025-01-17", "2025-01-18", "2025-01-19"],
    "lastMemo": "2025-01-19"
  }
}
```

**`oral-chapter-masks` 格式：**
```json
{
  "ch1": {
    "ru": true,
    "zh": false
  },
  "ch2": {
    "ru": false,
    "zh": false
  }
}
```

**`oral-speak-daily` 格式：**
```json
{
  "2025-01-15": [
    {"rowId": "row-1-1", "rating": 3, "timestamp": 1736940000000},
    {"rowId": "row-1-2", "rating": 4, "timestamp": 1736943600000}
  ],
  "2025-01-16": [
    {"rowId": "row-1-1", "rating": 5, "timestamp": 1737026400000}
  ]
}
```

**`oral-daily-position` 格式：**
```json
{
  "2025-01-15": 1250,
  "2025-01-16": 3800
}
```

---

## 三、数据迁移与兼容性

### Zlatoust 语法词汇答题记录

| 键名 | 数据类型 | 格式说明 | 写入位置 | 读取位置 | 用途 |
|------|-----------|----------|----------|----------|------|
| `rr_b2_progress_v1` | Object | `{bookId:unitId: {exerciseId: {selected, submitted, attempts, wrong, everWrong, lastResult, lastAnsweredAt, ...}}}` | reader.html | reader.html | 题目作答、错题和单元进度；Zlatoust 使用 `zlatoust_grammar:gl1` 至 `zlatoust_grammar:gl5` |
| `zlatoust_grammar_progress_answer_repair_v1_backup` | Object | `{version, createdAt, records}`，仅含五章 Zlatoust 迁移前记录 | reader.html | 仅供故障排查 | 首次答案键修正迁移的只读备份 |
| `zlatoust_grammar_progress_answer_repair_v1` | Object | `{version, migratedAt, correctedRecords}` | reader.html | reader.html | 迁移完成标记，保证重复打开页面不会重复处理 |

**答案键修正迁移：** 五章共 88 条答案键修正。首次读取到受影响的已提交单选记录时，`js/zlatoust-progress-migration.js` 会先备份五章记录，再按修复账本重算 `wrong` 和 `lastResult`。它保留 `selected`、`attempts`、`everWrong`、答题时间、开放作答和未知的未来字段；不处理俄语 B2 的其他模块、未提交选择或 `open-response` 记录。

### 版本迁移记录

**V7.0 → V9.0：**
- `oral-read-records` 旧格式：`{row-id: {read: bool, date: string}}`
- V9.0 新格式：`{row-id: {count: number, dates: [], lastRead: string}}`
- 迁移函数：`OralLib._migrate()` （在俄语知识库.html 中）

### 迁移代码示例

```javascript
_migrate: function() {
    try {
        var d = JSON.parse(localStorage.getItem('oral-read-records') || '{}');
        for (var k in d) {
            if (d[k].read && !d[k].count) {
                d[k] = {
                    count: 1,
                    dates: [d[k].date || ''],
                    lastRead: d[k].date || ''
                };
            }
        }
        localStorage.setItem('oral-read-records', JSON.stringify(d));
    } catch(e) {}
}
```

---

## 四、数据管理操作

### 导出数据

**函数：** `exportAllData()` （在 study-stats.html 中）

**导出内容：** 所有上述键名的当前值

**文件格式：** JSON，格式化输出（2空格缩进）

**文件名：** `mystudyspace-backup-YYYY-MM-DD.json`

### 导入数据

**函数：** `importData(event)` （在 study-stats.html 中）

**支持格式：** JSON 文件，结构与导出格式一致

**注意：** 导入会覆盖当前所有数据

### 清空数据

**函数：** `clearAllData()` （在 study-stats.html 中）

**操作：** 删除所有上述键名

**注意：** 此操作不可恢复，会弹出确认对话框

---

## 五、跨页面数据同步

### Storage 事件监听

**监听位置：** `study-stats.html`

**监听键名：**
- `pomodoro-today-count`
- `pomodoro-today-minutes`
- `pomodoro-total-count`
- `pomodoro-total-minutes`
- `pomodoro-streak`
- `oral-read-records`
- `oral-speak-records`
- `oral-memo-records`
- `oral-speak-daily`

**同步机制：** 当上述键名在其它页面（pomodoro.html 或 俄语知识库.html）中被修改时，会自动触发 `storage` 事件，`study-stats.html` 接收到事件后自动刷新统计数据显示。

### postMessage 通信

**用例：** 番茄钟迷你模式中的位置同步

**发送方：** `pomodoro.html`

**接收方：** `index.html`

**消息格式：** `'CLOSE_RUSSIAN'` （返回番茄主页）

---

## 六、调试与查看数据

### 浏览器控制台查看

```javascript
// 查看所有存储的数据
for (var i = 0; i < localStorage.length; i++) {
    var key = localStorage.key(i);
    console.log(key + ':', JSON.parse(localStorage.getItem(key)));
}

// 查看特定键
JSON.parse(localStorage.getItem('oral-read-records'))

// 清空所有数据
localStorage.clear()

// 删除特定键
localStorage.removeItem('oral-read-records')
```

### 推荐 Chrome 扩展

- **LocalStorage Explorer** — 可视化查看和编辑 localStorage
- **Storage Area Explorer** — 支持 localStorage 和 sessionStorage

---

## 七、注意事项

1. **数据持久性：** localStorage 数据永久存储，除非手动清除或浏览器清除缓存
2. **跨域限制：** localStorage 仅限同源页面访问
3. **存储上限：** 大多数浏览器限制为 5-10MB
4. **隐私模式：** 无痕模式下，localStorage 数据在关闭窗口后会被清除
5. **数据备份：** 建议定期使用"导出所有数据"功能备份

---

## 八、文件读写位置总结

| 文件名 | 读取的键名 | 写入的键名 |
|--------|------------|------------|
| `pomodoro.html` | 所有 `pomodoro-*` 键 | 所有 `pomodoro-*` 键 |
| `俄语知识库.html` | `oral-*` 所有键 | `oral-*` 所有键 |
| `study-stats.html` | 所有键（只读，用于显示统计） | 无（导出/导入/清空操作除外） |
| `index.html` | 无（仅负责 iframe 调度） | 无 |

---

## 九、版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V9.0 | 2025-01-15 | 初始版本，整合番茄钟和俄语学习数据文档 |
| V9.3 | 2025-01-20 | 新增 `study-stats.html` 统计面板，补充 `oral-speak-daily` 详细格式 |

---

*本文档会随着系统升级持续更新。*
