# 📋 俄语知识库 V7.0 排版 UI 审查报告

> **扫描时间**：2026-05-08
> **目标文件**：`俄语知识库.html`（475 KB / ~7200 行 / 179 个表格 / ~2200 条素材）

---

## 一、文件概览

| 指标 | 数值 |
|------|------|
| 文件大小 | 475 KB (~486,426 bytes) |
| 总行数 | ~7,200+ 行 |
| 表格总数 | **179 个** |
| `<th>` 表格头 | **442 个**（45 种唯一命名） |
| `<blockquote>` 使用 | **58 处** |
| `chapter-nav` 导航栏 | **18 处** |
| 词汇小灶章节 | **16 处** |
| 超长单元格 (>150字符) | **75 处** |
| p+strong 对话格式 | **135 处** |
| 带角色列的表格 | **22 处** |

---

## 二、问题分级总览

```
P0 严重 (影响阅读)  ████████████████░░  3 个
P1 中等 (影响美观)  ██████████████████  4 个
P2 细节 (打磨优化)  █████████████████░  4 个
───────────────────────────────
总计                ██████████████████  11 个
```

---

## 三、P0 — 严重影响阅读体验（3 个问题）

### #1 表格列标题不统一（3 种「俄语原文」变体混用）

**问题描述**：

179 个表格的 442 个表头中，「俄语原文」列存在 3 种不同命名变体，另有中文列名也存在多种写法。

**变体统计**：

| 列标题变体 | 出现次数 | 分布位置（行号） |
|-----------|----------|------------------|
| `俄语原文` | 101 | 大量分散在各章节 |
| `俄语原文（带重音）` | 14 | 420, 607, 664, 1108, 1179, 1260, 3194, 3219, 3981, 4010, 4222, 4271, 5627, 5657 |
| `俄语原文（男性口吻）` | 9 | 3800, 6795, 6853, 6903, 6970, 7029, 7092, 7163, 7206 |

**中文列名同样不统一**：

| 变体 | 次数 |
|------|------|
| 中文对照 | 124 |
| 中文原文 | 6 |
| 中文 | 22 |

**词汇小灶列结构也有两种模式**：

| 模式 | 列名组合 | 使用次数 |
|------|----------|----------|
| 类型 A | `俄语` + `中文` + `常见搭配/场景` | 14 |
| 类型 B | `俄语原文` + `中文对照` + `固定搭配` | 2 |

**修复建议**：
- 统一所有表格列标题命名规范
- 「俄语原文」系列统一为一种写法（推荐保留语义信息，如统一为 `俄语原文（带重音）`）
- 词汇小灶统一为 `俄语` + `中文` + `常见搭配/场景`

---

### #2 超长内容撑破单元格（75 处超长 TD）

**问题描述**：

75 处 `<td>` 单元格内容超过 150 字符，部分极端情况单格达到 ~700 字符且无换行，导致手机端需要横向滚动。

**最严重的超长内容**：

| 行号 | 字符数 | 内容摘要 |
|------|--------|----------|
| ~540 | ~600+ | 充电宝丢失完整叙述（含找东西过程 + 心理活动） |
| ~548 | ~700+ | 充电宝丢失后续段落（完整叙述经历 + 阿姨收起来的可能性） |
| ~556 | ~250 | 充电宝是妈妈的不是自己的 |
| ~796 | ~170 | 如果住自己家或一个人住的假设句 |
| ~804 | ~160 | 最近陪姐姐聚会的描述 |

**超长内容分布规律**：
- 场景对话的展开叙述段落
- 充电宝丢失话题的完整对话流
- 假设条件和原因解释的长复合句

**修复建议**：
- 对超过 200 字符的内容在适当位置插入 `<br>` 分段
- 或将超长叙述拆分为多个独立单元格/条目
- 确保 CSS 的 `word-break: break-word` 和 `overflow-wrap: break-word` 已生效（当前已设置，但长句无空格时仍可能失效）

---

### #3 `min-width:900px` 对移动端不友好

**问题描述**：

CSS 第 1 行 `body` 选择器中设置了 `min-width: 900px`，该值强制浏览器最小宽度为 900px。

```css
/* 位置：第 1 行 <style> 内 */
body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", ...;
    max-width: 92%;
    min-width: 900px;   /* ← 问题所在 */
    margin: 0 auto;
    padding: 25px 35px 60px;
    background: #f0f2f5;
    color: #2c3e50;
    line-height: 1.7;
}
```

**影响范围**：
- 手机端（<768px）会触发强制水平滚动
- 当前已有的 `@media (max-width: 768px)` 断点未覆盖此 `min-width`
- 用户在移动设备上阅读时体验极差

**修复建议**：
- 方案 A：直接移除 `min-width: 900px`（最简单）
- 方案 B：在 `@media (max-width: 768px)` 中添加 `body { min-width: 0; }` 覆盖（兼容性更好）
- 同时配合表格 `overflow-x: auto` 保证窄屏下表格仍可滚动查看

---

## 四、P1 — 影响美观和一致性（4 个问题）

### #4 章节导航栏 inline style 重复书写（18 处）

**问题描述**：

每个章节底部都有 `chapter-nav` 导航栏，全部使用完全相同的 inline style，样式代码重复了 18 次。

**inline style 内容（18 处完全一致）**：

```html
<div class="chapter-nav" style="display:flex;justify-content:space-between;align-items:center;
margin:0 0 14px 0;padding:8px 14px;background:#f6f7ff;
border-radius:8px;font-size:0.85em">
```

**分布位置（行号）**：414, 1102, 1890, 2791, 2915, 3188, 3728, 3975, 4216, 4562, 5022, 5411, 5959, 6348, 6404, 6483, 6626, 6720

**修复建议**：
- 将 inline style 提取为 CSS class `.chapter-nav { ... }`
- 全局只需定义一次，18 处 HTML 中移除内联样式

---

### #5 目录卡片展开态视觉反馈弱

**问题描述**：

点击目录卡片展开后，视觉变化仅限于：
- 边框颜色从 `#e8ecf1` → `#4F46E5`（微变）
- 阴影加深（`box-shadow` 从 `0 2px 8px rgba(...,0.08)` → `0 2px 12px rgba(...,0.12)`）
- 背景色从 `#fafbfc` → `#fff`（几乎无感知差异）

**当前展开态样式 vs 收起态对比**：

| 属性 | 收起态 | 展开态 | 差异程度 |
|------|--------|--------|----------|
| border-color | `#e8ecf1` | `#4F46E5` | ⭐⭐ 中等 |
| box-shadow | `0 2px 8px rgba(79,70,229,0.08)` | `0 2px 12px rgba(79,70,229,0.12)` | ⭐ 微弱 |
| background | `#fafbfc` | `#fff` | ⭐ 几乎不可见 |
| 左侧指示条 | 无 | 无 | ❌ 缺失 |

**修复建议**：
- 展开态增加左侧 3px 紫色指示条（与 h3 标题风格呼应）
- 展开态背景色改为更明显的淡紫 `#f6f7ff`
- 展开/收起按钮旋转动画已有（✓），可保留

---

### #6 表格条纹色差太小（odd/even 几乎不可辨）

**问题描述**：

当前表格斑马纹配色对比度不足：

```css
tr:nth-child(odd) td  { background: #fff; }
tr:nth-child(even) td { background: #f8f9fc; }
```

`#fff` 与 `#f8f9fc` 在普通屏幕上几乎无法区分，导致表格行间缺乏层次感。

**修复建议**：
- 加深 even 行颜色至 `#f0f2f5` 或 `#f3f4f8`
- 或采用微妙的主题色调：`even → rgba(79,70,229,0.04)`

---

### #7 `<blockquote>` 样式未统一设计（58 处用于 4 种语义）

**问题描述**：

58 处 `<blockquote>` 被用于 4 种完全不同的语义场景，但全部共用同一套基础样式，缺乏语义化区分。

**4 种使用模式**：

| 模式 | 用途 | 数量 | 示例位置 |
|------|------|------|----------|
| A. 章节说明 | 每章开头的涵盖说明 | 14 | `涵盖：起床/睡觉/熬夜... \| 语体：ты形式` |
| B. 场景描述 | B2 口试练习场景提示 | 5 | `?? 场景：在一个大学图书馆里...` |
| C. 对话翻译 | 中文翻译/对照文本 | ~39 | `你们那边已经凌晨1点了！？是的。` |
| D. 考试评分说明 | TRKI 评分重点 | 3 | `ТРКИ-B2口试评分重点：...` |

**修复建议**：
- 为每种语义添加独立 class：`.bq-chapter-info` / `.bq-scene-hint` / `.bq-translation` / `.bq-exam-tip`
- 各 class 配合不同的左边框颜色或图标前缀做视觉区分
- 例如：章节说明用紫色边框、场景提示用青色、翻译用灰色、考试说明用橙色

---

## 五、P2 — 细节打磨建议（4 个问题）

### #8 俄语重音符号 `U+0301` 渲染不稳定

**问题描述**：
- 俄语重音使用 Combining Acute Accent (`́`) 与字母组合显示
- 在部分字体/浏览器组合中，重音符号可能出现垂直偏移或重叠
- 当前俄语列字体设置为 `'PT Serif', serif`，该字体对组合字符的支持因平台而异

**修复建议**：
- 测试并考虑指定 fallback 字体序列中对组合字符支持更好的选项
- 或在关键位置使用预组字符（如 `а́` → `á` 风格的预组形式），但这对于西里尔字母选择有限
- 更实际的方案：确保字体栈包含系统原生支持好的字体

---

### #9 场景对话呈现方式不统一（3 种格式混用）

**问题描述**：

场景对话部分存在三种不同的 HTML 结构模式：

| 格式类型 | 结构 | 使用次数 | 示例 |
|----------|------|----------|------|
| p+strong 格式 | `<p><strong>角色</strong>：内容</p>` | 135 | 主流格式 |
| blockquote 格式 | `<blockquote>中文翻译</blockquote>` | 42 | 通常跟在 p+strong 后面作为翻译 |
| table 格式 | 带「角色」列的表格 | 22 | 少量章节使用 |

**注意**：p+strong + blockquote 的组合实际上是一种「双行模式」（俄语一行 + 中文翻译一行），而 table 格式则将角色/俄语/中文放在同一行的三列中。这两种模式的阅读体验和信息密度差异较大。

**修复建议**：
- 如果追求一致性：全局统一为 p+strong + blockquote 双行模式（更适合语言学习——原文和译文分行显示，减少干扰）
- 如果追求紧凑：统一为表格模式
- 不建议维持现状的混用状态

---

### #10 词汇小灶第三列「常见搭配/场景」列宽不足时溢出

**问题描述**：
- 第三列内容通常较长（搭配短语、使用场景描述）
- 当内容超出分配宽度时，虽然 CSS 设置了 `word-break: break-word`，但视觉效果不佳
- 三列宽度分配可能不均（第一列俄语 52% 固定宽度后剩余空间分给其他两列）

**修复建议**：
- 给第三列设置 `min-width: 25%` 保证最低宽度
- 或改用弹性布局让第三列获得更多空间

---

### #11 中俄文混排视觉重量不平衡

**问题描述**：
- 西里尔字母（俄文）笔画密度远高于汉字（中文），同等字号下俄文看起来更「重」「黑」
- 当前 `td:first-child`（俄语列）设置了 `width: 52%` + `font-family: 'PT Serif', serif` + `font-size: 16px`
- 中文列使用默认字体和较小字号（`.94em`）
- 两列放在一起时视觉重量感不平衡

**修复建议**：
- 适当降低俄语列字重（`font-weight: 400` 而非默认的 500+）
- 或微调两列字号差距使视觉更协调
- 增加行高补偿俄文的密集感

---

## 六、关键数据速查表

| 元素类型 | 数量 | 备注 |
|----------|------|------|
| `<table>` 表格 | 179 | 含速查表、句型表、对话表等 |
| `<th>` 表头 | 442 | 45 种唯一命名 |
| `<blockquote>` | 58 | 4 种语义用途 |
| `chapter-nav` | 18 | 全部相同 inline style |
| 词汇小灶章节 | 16 | 2 种列结构变体 |
| 超长 TD (>150字符) | 75 | 最长 ~700 字符 |
| p+strong 对话 | 135 处 | 主流对话格式 |
| 带角色列表格 | 22 | 替代对话格式 |
| 目录核心卡片 | 13 | 对应 13 大话题 |

---

## 七、建议修复方案

### 方案 A — 仅修 P0（快速止血）⚡

| 修复项 | 改动范围 | 风险 |
|--------|----------|------|
| 统一表格列标题 | ~124 处 th 文本替换 | 低 — 纯文本修改 |
| 拆分超长单元格内容 | ~20~30 处插入换行 | 低 — 仅加 `<br>` |
| 修复 min-width 移动端断点 | 1 行 CSS | 极低 |

**预计工作量**：影响 ~150~200 行代码
**适合场景**：想快速解决最影响阅读的问题

---

### 方案 B — P0 + P1 全量修复 🔧

在方案 A 基础上额外修复：

| 修复项 | 改动范围 | 风险 |
|--------|----------|------|
| chapter-nav CSS 化 | 18 处删 inline style + 新增 CSS class | 低 |
| 表格条纹色差调整 | 1 行 CSS 色值修改 | 极低 |
| blockquote 语义化分类 | 58 处添加 class + 新增 CSS | 低 |
| 目录展开态增强 | CSS 修改 | 极低 |

**预计工作量**：额外 ~80 行代码
**适合场景**：希望整体视觉一致性提升

---

### 方案 C — 全面精修 P0-P2 ✨

在方案 B 基础上再打磨细节：

| 修复项 | 改动范围 | 风险 |
|--------|----------|------|
| 对话格式统一 | ~100+ 处 HTML 结构调整 | 中 — 需仔细核对内容 |
| 视觉重量平衡调优 | CSS 字号/字重微调 | 低 |
| 重音字体优化 | CSS font-stack 调整 | 低 |
| 词汇小灶列宽优化 | CSS 调整 | 低 |

**预计工作量**：额外 ~150~200 行代码
**适合场景**：准备发布正式版本前的全面精修

---

## 八、附录：各章节 chapter-nav 行号索引

| 序号 | 章节 | 行号 |
|------|------|------|
| 1 | 一、日常起居与生活琐事 | ~414 |
| 2 | 二、学习·考试·留学规划 | ~1102 |
| 3 | 三、饮食·健康·生活习惯 | ~1890 |
| 4 | 四、情绪表达与共情互动 | ~2791 |
| 5 | 五、天气·季节·自然环境 | ~2915 |
| 6 | 六、家庭·朋友·人际关系 | ~3188 |
| 7 | （中间过渡章节） | ~3728 |
| 8 | 七、旅行·出行·交通 | ~3975 |
| 9 | 八、工作·兼职·职业规划 | ~4216 |
| 10 | 九、问候·寒暄·万能回应 | ~4562 |
| 10 | 十、兴趣爱好·文化·科技 | ~5022 |
| 11 | 十一、语言学习与交流技巧 | ~5411 |
| 12 | 十二、计划与未来安排 | ~5959 |
| 13 | 十三、B2 口试专项 | ~6348 |
| 14 | 模块A：功能句型 | ~6404 |
| 15 | 模块B：场景应对 | ~6483 |
| 16 | 模块C：重音速查 | ~6626 |
| 17 | 附录/补充 | ~6720 |

---

*报告生成时间：2026-05-08 21:47 · 扫描工具：WorkBuddy Code Explorer*
